{% extends 'email/email.html.twig' %}

{% block main %}
    <p>
        <a href="{{ host ~ base_path() ~ url_for(languageSlug ~ '-details', {'id': added.slug}) }}">
            {{- added.name -}}
        </a> wurde eingetragen.
    </p>
{% endblock %}
